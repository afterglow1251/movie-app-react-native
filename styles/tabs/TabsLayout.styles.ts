import { colors } from "@/constants/colors";
import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  tabBarItem: {
    paddingVertical: 10,
  },
  tabBar: {
    backgroundColor: colors.corbeau,
    borderColor: colors.corbeau,
    height: 100,
  },
});
